<%= form_with model: Post do |form| %>
  <div>
    <%= form.text_field :title, placeholder: 'Post Title' %>
  </div>
  <div>
    <%= form.text_area :text, placeholder: 'Enter post.......' %>
  </div>
  <div>
    <button type="submit">Post</button>
  </div>
<% end %>